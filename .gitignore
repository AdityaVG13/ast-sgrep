/target/
**/.asgrep/
.internal/
*.db
*.db-*

# OS files
.DS_Store
Thumbs.db

# Repository-root path/index noise
/~/
/LanguageClient/

# Node package installs
node_modules/

# Editor state
*.swp
*.swo
*~
.idea/
*.iml
.vscode/

# Environment files (never commit secrets)
.env
.env.*
!.env.example

# Merge/backup artifacts
*.orig
*.bak
*.tmp
*.log

# Local tool binaries
.tools/
rust_out

# Local skill / agent / tool harness state (never product surface)
.adversarial-review/
.code-upgrade/
.cyclomatic-reduction/
.math-review/
.ub-exorcism/
.verification-purpose/
.zero-tech-debt/
.upgrade-backup/
.cursor/
.tokenzero/
.fszero/
.zerostack/
.zerostack.*/
.unsafe-audit/
.prosecution/
agent_ergonomics_audit/
.agents/
.claude/
.codex/
# Local agent instruction dumps (optional private harness; not product surface)
/AGENTS.md
/CLAUDE.md
/GEMINI.md
.skill-loop-progress.md
.doctor/
.bv/
.dolt/
**/.tokenzero/
**/.fszero/
**/.zerostack/

# Accidental shell-expanded directory names
${*}

# library-updater / verification debris at repo root
UPGRADE_LOG.md
UPGRADE_LOG*.log
claude-upgrade-progress.json
verification-*.log
RECOVERY_RUNBOOK.md
SYNC_STRATEGY.md

# Cargo package metadata
**/.cargo_vcs_info.json

# Benchmark: only markdown docs are tracked under benchmarks/
# Keep any accidental local harness/scratch out of git
benchmarks/**/__pycache__/
benchmarks/corpora/
benchmarks/results/*
!benchmarks/results/*.md
benchmarks/tmp-idx/
benchmarks/cold-start-work/
benchmarks/query-path-work/
benchmarks/perf/
__pycache__/

# beads local issue store
.beads/
.beads-bd-backup-*/
.beads-credential-key

# Fuzz / editor build artifacts
fuzz/target/
fuzz/Cargo.lock
editors/**/node_modules/
editors/**/out/
editors/**/*.vsix

# Tests -- local dumps only; never ship
tests/artifacts/
# Golden mismatch dumps (assert_golden writes these on failure; never commit)
*.actual

# Local bench history scratch (committed SSoT is `.bench-history/*.latest.json`)
.bench-history.json
**/.bench-history.json
.bench-history/*.run.json

# Agent skill specs (local-only)
.agents/

# Agent / tool harness scratch (never product surface)
.cline/
.gemini/
.opencode/
.windsurf/
.pi-subagents/
.ee/
.worktrees/
/_*.txt
/.bead-count.txt
/passes/

# Internal research notes (not product docs)
/CHANGELOG_RESEARCH.md

# Fuzz corpora are regenerable seeds — keep the targets, not the corpus
fuzz/corpus/

# CLI benchmark-history state (regenerated on runs)
**/.bench-history.json

# Local tool/audit state (not product)
.house-odds/**/.fszero/
/beads_compliance_audit/

# state-space-collapse skill artifacts
.state-space-collapse/
/.papercuts.jsonl
/.skill-loop-progress-*.md

# module-coupling-topology skill artifacts (local books)
.module-coupling-topology/
.rotational-code-analysis/
.rotational-code-analysis
*.rotational-code-analysis/
.code-upgrade-enterprise/
