## Summary

<!-- What changed and why. -->

## Test plan

- [ ] Behavior is covered by a targeted test (or an existing one still applies)
- [ ] Golden / fixture diffs reviewed file-by-file (no host paths)
- [ ] No committed `*.actual` files
- [ ] Metric / latency / MRR claims cite `benchmarks/results/baselines.md` (fingerprint + status) or are tagged `UNREPRODUCIBLE`
- [ ] CI stays compare-only (`ASGREP_UPDATE_GOLDENS` is never `1` under `.github/`)
