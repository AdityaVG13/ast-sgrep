# Pass 5/10 — Fixtures, Round-Trip, Provenance

**Repo:** `/Users/aditya/Developer/ast-sgrep`  
**Branch:** `perf/software-optimization` (audit only; no beads, no implementation, no commits)  
**Date:** 2026-08-07  
**Skill:** `testing-conformance-harnesses` (Patterns 2–3: golden + round-trip; Fixture Provenance; DISCREPANCIES rules)  
**Prior:** PASS1–PASS4 under `tests/artifacts/conformance-audit/`  
**Cross-ref:** golden-artifacts program under `tests/artifacts/golden-audit/` (owns `assert_golden` / `ASGREP_UPDATE_GOLDENS` design; this pass inventories fixtures + round-trips only)

**Search coverage:** `tests/fixtures/**`, `crates/*/tests/fixtures/**`, ranking `cases.json`, extract inputs, CLI machine JSON, loaders in testkit/core/cli/lang, `semantic_ivf_roundtrip` + property/durability round-trips, PROVENANCE/DISCREPANCIES/COVERAGE absence, golden-audit PASS1–2/5–6 for update env naming. Shell + `rg`; ZeroStack (`zs 1.3.0`) used for inventory where permit allowed.

---

## 1. Executive summary

| Dimension | Status |
|-----------|--------|
| Checked-in fixture trees | **3 roots** + inline temp corpora in tests |
| Provenance grade overall | **partial at best** — no `PROVENANCE.md` for any test fixture tree |
| `DISCREPANCIES.md` / `COVERAGE.md` | **Absent** (confirmed Pass 1–4; still true) |
| Shared golden update path | **Absent** — see golden-audit; env name **`ASGREP_UPDATE_GOLDENS`** |
| Strongest Pattern-3 suite | IVF wire round-trip (`semantic_ivf_roundtrip.rs`) |
| Strongest fixture→oracle loop | Ranking `cases.json` + `ranking_oracle.rs` |
| Strongest Pattern-2 freeze files | CLI machine JSON (`capabilities` / `envelopes` / `machine_shapes`) |
| Shared sample corpus | `tests/fixtures/sample/src/*` (8 langs); `.asgrep/` local DBs **gitignored** |

This pass is **inventory + recommendations only**. It does not re-spec `assert_golden` (golden-artifacts program owns that) and does not implement DISC/COVERAGE files.

---

## 2. Provenance grade rubric (this pass)

| Grade | Meaning |
|-------|---------|
| **none** | No source header, no generator command, no version pin, no review note |
| **partial** | Intentional structure and/or code comments / product docs; loader types; implicit pins (Cargo.lock) — but no formal PROVENANCE artifact |
| **strong** | Dedicated PROVENANCE (or fixture header block) with generator command, inputs, version pins, review/update notes |

**Repo-wide finding:** **0** files named `PROVENANCE.md` under test trees. Product docs `docs/signal-provenance.md` and benchmark honesty ledgers are **false friends** for fixture provenance.

---

## 3. Fixture inventory table

| Path | Kind | Consumers / loaders | Update path | Provenance grade | Notes |
|------|------|---------------------|-------------|------------------|-------|
| `tests/fixtures/sample/src/*` (8 files: rs/py/go/ts/rb/java/cs + lib.ts) | **Input corpus** | `testkit::sample_root` / `sample_file`; `index_sample`; CLI session; ranking oracle; many core/cli/lsp tests; `bench_suite` fixture `"sample"` | Hand-edit sources | **partial** | Shared multi-lang auth/process themes; no header block; no corpus fingerprint; intentional for ranking/graph smoke |
| `tests/fixtures/sample/.asgrep/index.db*` | **Ephemeral index** | Local runs only | Regenerated by indexer | **none** (and **not tracked**) | `**/.asgrep/` in `.gitignore` — correct |
| `tests/fixtures/.asgrep/index.db*` | **Ephemeral index** | Spurious root under fixtures | Same | **none** (gitignored) | Not a deliberate fixture; noise if present on disk |
| `tests/fixtures/ranking/cases.json` | **Oracle case table** (12 cases) | `crates/ast-sgrep-core/tests/ranking_oracle.rs` — path load + `deny_unknown_fields` | Hand-edit JSON; no UPDATE env | **partial** | Best retrieval oracle fixture; binds `"fixture": "sample"`; modes hybrid/semantic; soft `must_include` + `max_rank` — not full rank dump |
| `crates/ast-sgrep-cli/tests/fixtures/capabilities.json` | **Machine golden** (full value, scrubbed version) | `machine_contracts.rs` `fixture("capabilities")` via `include_str!` | Manual / future `ASGREP_UPDATE_GOLDENS` | **partial** | Strong freeze content; scrub `"version"` → `"<version>"`; no PROVENANCE header |
| `crates/ast-sgrep-cli/tests/fixtures/envelopes.json` | **Machine golden** (operational/usage/version) | `machine_contracts` | Same | **partial** | Placeholders `<command>`, `<message>`, `<version>`; single-line JSON |
| `crates/ast-sgrep-cli/tests/fixtures/machine_shapes.json` | **Key-set freeze** (structural) | `assert_shape` in machine_contracts | Same | **partial** | Keys for index/status/doctor/agent/agent-capsule/compact — not value-level |
| `crates/ast-sgrep-lang/tests/fixtures/extract/*` (13 langs) | **Extract input sources** | `extraction_goldens.rs` `include_str!` → `assert_language_conformance` | Hand-edit source + Rust tuples | **partial** | Inputs only; expectations live in Rust (`LanguageConformanceCase`); "GoldenWidget" names are **symbols**, not golden files (false friends per golden-audit) |
| Inline temp corpora (`graph_oracle`, MCP, metamorphic, properties, etc.) | **Synthetic per-test** | Written in test body | N/A (code is source of truth) | **none** as files; **partial** as documented tests | Correct for isolation; not shared fixtures |
| Ephemeral eval gold (temp JSON in machine_contracts) | **Eval GoldFixture** | `asgrep eval --gold` path; `eval.rs` `load_gold` | Test-written temp only | **none** checked-in | **No** multi-query gold fixture in tree for published baselines (honesty: baselines.md is UNREPRODUCIBLE ledger) |
| `packages/pi/release-contract.json` | **Release contract freeze** | `check-contract.mjs` | Hand / script regenerate | **partial–strong** | Outside `tests/fixtures`; version-coupled; not Pattern-3 |
| `benchmarks/results/baselines.md` | **Honesty ledger** | Agents.md rule only | Human | **strong for metrics**, **not** a test fixture | Do not treat as CI golden |

### 3.1 Loader styles (fragmented — Pass 2 F4 still true)

| Style | Location | Strength |
|-------|----------|----------|
| **A. Compile-time `include_str!`** | CLI machine fixtures; lang extract inputs | Byte-stable; no path escape; no UPDATE helper |
| **B. Path-loaded JSON** | ranking `cases.json` via `CARGO_MANIFEST_DIR` | Typed + `deny_unknown_fields`; multi-failure bag |
| **C. Canonical sample root** | `testkit/src/fixture.rs` → `../../tests/fixtures/sample` | Shared corpus; canonicalize |
| **D. Inline temp writers** | graph_oracle, factory, properties | Isolation; no external review artifact |
| **E. Eval gold path** | CLI `eval --gold` | Loader exists; **no checked-in multi-query gold** |

There is **no** shared fixture root resolver, naming convention doc, or PROVENANCE registry spanning A–E.

### 3.2 Ranking cases.json shape (reference)

- **12** cases: defs/callers/literal/nl/semantic synonym + per-lang def/caller constraints (rs, py, go, java, rb, csharp).
- Soft oracle only (`must_include` + `max_rank`); absolute RRF order **not** frozen (Pass 3).

### 3.3 What is *not* a checked-in fixture (gaps)

| Missing artifact | Why it matters | Prior pass |
|------------------|----------------|------------|
| Full extract dump goldens (JSON) per language | Grammar bumps can drop symbols silently | P1/P4 F4 |
| Versioned IVF binary frames (v1→v2) | Cross-version wire | P1/P3 S7 |
| Pre-v7 SQLite migration golden DBs | Schema reopen integrity | P3 S6 / B4 |
| Full search hit-array dumps for sample | Agent envelope completeness | P3 S1 |
| Differential corpus for pattern:×ast-grep / literal×rg | External honesty | P4 F1/F3; jell-deferral |
| `tests/golden/` or `**/snapshots/` layout | Golden-artifacts program | golden PASS1–2 |

---

## 4. Round-trip inventory and gaps

### 4.1 Inventory

| Suite / site | Pattern | What is asserted | Verdict form | Gaps |
|--------------|---------|------------------|--------------|------|
| **`semantic_ivf_roundtrip.rs`** (core integration test) | **P3 wire + corrupt reject + internal differential** | save→load vectors + fingerprint gate; wrong fp/generation reject; corrupt magic/version/header/clusters/reserved/trailing/truncated → None; atomic replace keeps old mapping; CE-003 IVF top-k ≡ brute force (n≥threshold); adaptive recall@10 budget; open p99 (env-gated); `#[ignore]` tradeoff SLO | panic + ignore + optional `ASGREP_PERF_ASSERTS` | **No** checked-in historical frames; **no** DISC for intentional format breaks; latency host-bound (doc in `semantic-ivf-mmap.md` has host provenance — product perf, not fixture PROVENANCE) |
| **`durability_epics.rs`** (subset) | P3 sidecar | IVF load after durability paths | panic | Overlaps IVF suite; not a separate wire corpus |
| **`properties.rs` `store_upsert_delete_roundtrip`** | Store lifecycle | index → remove_file → hash gone | panic | Name "roundtrip" = CRUD, not serialize types |
| **`capsule_format.rs`** (plugins) | Format serialize + compact path table | Compact identity decode; path fold round-trip; budget fields | assert_eq | Synthetic SearchResponse; **not** file goldens; good future golden consumer |
| **`machine_contracts` agent discovery** | Env/bool round-trip-free | Discovery defaults | assert | Naming only; not codec |
| **Codemode `plan.rs`** | serde plan parse | Runtime plan JSON | product code | "round-trips" in comments = agent tool chaining, **not** test Pattern-3 |
| **Index / SQLite** | Migrate open | SCHEMA_VERSION=7, pragmas, delete | various | **No** vN→7 golden DB corpus (Pass 3) |
| **Determinism loop** | Self-golden 50× | First JSON baseline in memory | panic | Proves search JSON can be frozen; **not** committed |

### 4.2 IVF suite test list (9 tests; 1 ignored)

| Test | Role |
|------|------|
| `invalidating_a_missing_sidecar_is_idempotent` | Idempotent invalidate |
| `semantic_ivf_roundtrip_and_fingerprint_gate` | **Core P3** vectors + fp + search parity |
| `save_rejects_an_index_for_a_different_vector_population` | Fail-closed writer |
| `mapped_reader_rejects_corrupt_or_truncated_frames_without_panicking` | Corrupt matrix |
| `mapped_reader_survives_atomic_sidecar_replacement` | Publication / mmap inode |
| `medium_mapped_sidecar_reports_open_p99` | Latency measure |
| `ivf_search_matches_brute_force_top_k_indices_ce003` | Internal math differential |
| `adaptive_ivf_recall_at_10_stays_within_quality_error_budget` | Threshold recall |
| `adaptive_ivf_tradeoff_at_2048_and_10000_vectors` | **`#[ignore]`** tradeoff |

**Targeted command (from `docs/validation/semantic-ivf-mmap.md`):**

```bash
ASGREP_PERF_ASSERTS=1 cargo test --locked --profile release-perf \
  -p ast-sgrep-core --test semantic_ivf_roundtrip -- \
  --nocapture --test-threads=1
```

### 4.3 Round-trip gaps (prioritized)

1. **No versioned binary IVF fixture corpus** (v1 frame reject + v2 accept) — synthetic-only today.  
2. **No SQLite migration golden DBs** (schema 1…6 → 7).  
3. **No extract/serialize dump round-trip** (extract → JSON → re-assert).  
4. **No machine JSON UPDATE path** — compare-only via hand-maintained files (golden program).  
5. **Eval gold loader without committed multi-query gold** — eval path unexercised as conformance fixture.  
6. **Serialize-types suite absent** as a named module — serde types covered ad hoc (plan, envelopes), not a codec matrix.

---

## 5. Provenance documentation status

| Artifact | Present? | Notes |
|----------|----------|-------|
| `PROVENANCE.md` under any fixture/golden tree | **N** | Confirmed search; golden-audit PASS2 same |
| Fixture file headers (generator, date, source) | **N** for JSON freezes; extract sources have ordinary language comments only | ranking `cases.json` has no header comment (JSON) |
| Generator / regenerate commands next to fixtures | **N** | Closest: product validation docs (`semantic-ivf-mmap.md`, `compact-output.md`) for **perf/format** not fixture regen |
| Grammar / lock pins on extract fixtures | **Implicit** Cargo.lock only | No meta in fixture tree |
| Sample corpus fingerprint | **N** | ranking binds name `"sample"` only |
| Benchmark number provenance | **Y** via `benchmarks/results/baselines.md` + Agents.md | Orthogonal to test fixtures |

### 5.1 Update workflows (cross-ref golden-artifacts; do not re-spec)

| Item | Status | Owner |
|------|--------|-------|
| Shared `assert_golden` | **Absent** | golden-artifacts PASS2 P0 |
| Update env | **`ASGREP_UPDATE_GOLDENS`** (truthy: 1/true/yes/on) | golden PASS2/PASS6 — **reject** bare `UPDATE_GOLDENS` / INSTA_UPDATE for product tests |
| CI compare-only pin | Not yet (no write path) | golden PASS6 B1 |
| `.gitignore` `*.actual` | **Y** | golden PASS2 |
| Manual update today | Edit fixture JSON / Rust tuples; re-run `cargo test` | de-facto |

Conformance harness work that freezes dumps should **call into** (or await) testkit `assert_golden` once landed — not invent a second env name.

---

## 6. Recommended `DISCREPANCIES.md` seed list

**Location (recommended):** `docs/validation/DISCREPANCIES.md` or `tests/conformance/DISCREPANCIES.md` (harness later loads IDs).  
**Rule:** each row = intentional product divergence or deferred claim, with evidence, not a bug list.

| ID (proposed) | Surface | Intentional divergence | Evidence today | Test / XFAIL posture |
|---------------|---------|------------------------|----------------|----------------------|
| **DISC-pattern-native-subset** | `pattern:` vs full ast-grep | No external process; nested rules/YAML/predicates unsupported → empty hits | `docs/structural-patterns.md`; Pass 4 F1 | Document + optional ⊆ oracle later; not a failure |
| **DISC-no-jell-harness** | External differential | jell / multi-engine equality **deferred** | `docs/validation/jell-deferral.md` | COVERAGE row "external differential = deferred" |
| **DISC-lexical-not-rg** | `literal:` / FTS vs ripgrep | Not byte-compatible with rg (ignores, binary, symlink policy) | jell-deferral + Pass 4 F3 | Any future rg compare needs this DISC first |
| **DISC-compact-drops-provenance** | compact format | Omits contributor/score/margin decoration; one-byte signal code | `docs/signal-provenance.md`, `docs/validation/compact-output.md` | Assert omission as **contract**, not bug |
| **DISC-casefold-ascii** | Graph/symbol queries | Case-fold matrix is ASCII-oriented (graph_oracle mixed-case Latin) | `graph_oracle.rs` | Non-ASCII fold = out of contract until expanded |
| **DISC-ranking-soft-oracle** | Hybrid rank | Soft `must_include`/`max_rank` only — not absolute RRF order | `cases.json` + Pass 3 | Do not claim full rank conformance |
| **DISC-extraction-presence-only** | Lang extract | Presence/forbid tuples, not full span/order dumps | `extraction_goldens` + testkit | Grammar drift may need DISC rows per bump |
| **DISC-ivf-adaptive-recall** | ANN | Adaptive probes trade recall; ignore-marked SLO tradeoff | `semantic_ivf_roundtrip` ignore + CE-003 exact path | XFAIL/ignore already; register formally |
| **DISC-baselines-unreproducible** | Bench metrics | Published MRR/etc. historical UNREPRODUCIBLE | Agents.md + `baselines.md` | Never CI-assert as golden |
| **DISC-mcp-not-full-suite** | MCP | Process tests vs protocol pin; not official MCP compliance suite | Pass 2 MCP section | Avoid "MCP conformant" marketing language |

Seed prose can start from Pass 2 F2 / Pass 4 F2 and `jell-deferral.md` without new product decisions.

---

## 7. Recommended `COVERAGE.md` skeleton

**Purpose:** living matrix of MUST/SHOULD clauses × tests × status (Pass 3 residual B5).  
**Suggested path:** `docs/validation/COVERAGE.md` (or harness-emitted report later).

```markdown
# Conformance coverage matrix

Last updated: YYYY-MM-DD
Sources: docs/validation/*, QUERY_GRAMMAR, structural-patterns, signal-provenance, machine-json-schema
Harness: (none unified yet — see conformance-audit PASS2)

## Legend
| Status | Meaning |
| covered | Automated test asserts the clause |
| partial | Soft oracle / subset / single surface |
| gap | No automated guard |
| disc | Covered by DISCREPANCIES.md (intentional non-claim) |
| deferred | Explicitly postponed (e.g. jell) |

## Surfaces

### S1 Machine JSON (schema 1.0.0)
| Clause ID | MUST/SHOULD | Test | Status | Notes |
|-----------|-------------|------|--------|-------|
| MJ-001 tool==asgrep | MUST | machine_contracts | covered | |
| MJ-002 schema_version | MUST | machine_contracts + fixtures | covered | |
| MJ-0xx full hit-array freeze | MUST | — | gap | Pass 3 |

### S2 Query grammar
| … | | | gap/partial | |

### S3 pattern: subset
| PG-native | MUST | pattern.rs smoke | partial | DISC-pattern-native-subset |

### S4 Ranking / fusion
| RK-soft | SHOULD | ranking_oracle cases.json | partial | DISC-ranking-soft-oracle |

### S5 Graph / chain
| … | | graph_oracle | partial | |

### S6 Index schema v7 / migrations
| IX-migrate | MUST | durability/store tests | partial | golden DBs gap |

### S7 IVF wire v2
| IVF-roundtrip | MUST | semantic_ivf_roundtrip | covered | |
| IVF-v1-corpus | SHOULD | — | gap | |

### S8 Signal provenance multi-surface
| SP-fields | MUST | signal_provenance + surface smokes | partial | DISC-compact-drops-provenance |

### S9 Lang extraction (13 langs)
| EX-presence | SHOULD | extraction_goldens | partial | DISC-extraction-presence-only |

### S10 MCP / LSP / Pi
| … | | protocol.rs / lsp / release-contract | partial | |

## Deferred external differentials
| Item | DISC id | Status |
| jell multi-engine | DISC-no-jell-harness | deferred |
| pattern × ast-grep | DISC-pattern-native-subset | deferred (P4 F1) |
| literal × rg | DISC-lexical-not-rg | deferred (P4 F3) |

## How to regenerate this file
(future) harness --coverage-report; until then: hand-update when closing beads.
```

**Link from:** `docs/validation/proof-pack.md`, CONTRIBUTING test bar, and future conformance harness README.

---

## 8. Aggregated findings for beads (max 4 deep)

Fold with prior themes; **do not file** in this pass.

| # | Theme | Fold-with | Why deep (not pure inventory) | Suggested acceptance (audit-level) |
|---|-------|-----------|-------------------------------|-------------------------------------|
| **1** | **Fixture provenance + fragmentation** | Pass 2 F4; golden PASS2 PROVENANCE gap | Three loader styles (include_str / path JSON / inline temp) with **zero** PROVENANCE.md; sample corpus unfingerprinted; machine freezes lack headers/generator | Add `tests/fixtures/PROVENANCE.md` (+ per-crate fixture notes); document regenerate SOP; optional sample fingerprint field in ranking cases; cross-link golden `assert_golden` when present |
| **2** | **Round-trip corpus gaps (IVF + schema migrations)** | Pass 1 top-7; Pass 3 S6/S7 | IVF suite is strong **in-process**; missing **on-disk versioned frames** and **pre-v7 DB goldens** for reopen/migrate | Small checked-in `tests/fixtures/ivf/` frames (v2 good + corrupt samples optional) + `tests/fixtures/migrations/` minimal DBs; wire tests fail-closed on open |
| **3** | **DISCREPANCIES.md seed registry** | Pass 2 F2; Pass 3 B2/B3; Pass 4 F2 | Intentional divergences already documented in product docs but not loadable as DISC-NNN; "conformance" language overclaims | Land seed file from §6; cite from structural-patterns, jell-deferral, signal-provenance, compact-output |
| **4** | **COVERAGE.md + oracle→matrix emitter** | Pass 3 B5; Pass 4 residual | Ranking cases + machine goldens + extract tuples exist but no clause IDs / living matrix | Skeleton §7 + optional IDs on ranking cases / machine tests; emit coverage table in proof-pack or CI artifact later |

**Out of scope / already owned by golden-artifacts program (cross-ref only):**

- Implement `assert_golden` + `ASGREP_UPDATE_GOLDENS` write path  
- Scrubber registry / `*.actual` upload  
- insta vs custom choice  
- CI PR triggers for golden jobs  

**Do not re-open:** honesty baselines as CI goldens; renaming extraction presence suites without adding dumps.

---

## 9. Protect list (do not regress)

1. **Ranking `cases.json` + deny_unknown_fields + multi-case failure bag** — best retrieval fixture→oracle loop.  
2. **CLI machine compile-time fixtures + scrub placeholders** — strongest Pattern-2 freezes.  
3. **IVF round-trip / corrupt matrix / CE-003 non-vacuous threshold** — strongest Pattern-3.  
4. **testkit `sample_root` + private TempDir indexes** — shared corpus without committing DBs.  
5. **jell-deferral + structural-patterns honesty** — seed DISC rows rather than fake equality.  
6. **`.gitignore` `**/.asgrep/` and `*.actual`** — keep ephemeral indexes and mismatch dumps out of git.

---

## 10. Explicit non-goals (this pass)

- No implementation of harnesses, goldens, DISC/COVERAGE files  
- No bead filing, no commits  
- No re-spec of `assert_golden` (golden-artifacts)  
- No full test suite run (source + fixture inventory only)  
- Did not treat product `docs/signal-provenance.md` as fixture PROVENANCE  

---

## 11. Summary scores (subjective, audit)

| Dimension | Score (1–10) | Note |
|-----------|--------------|------|
| Fixture inventory richness | **6** | Enough corpora for smoke/oracles; thin for dumps/migrations |
| Loader quality | **7** ranking/machine/lang; **4** unified discipline | Fragmented but intentional |
| Round-trip (P3) | **8** IVF; **3** elsewhere | IVF is the star |
| Provenance documentation | **2** | No PROVENANCE.md; partial implicit |
| DISC / COVERAGE readiness | **1** files; **6** source material | Docs already know the divergences |
| Update workflow | **2** de-facto manual; **0** shared UPDATE | Golden program tracks the path |

**Overall fixture/round-trip/provenance maturity for conformance:** ~**4/10** — strong islands (machine JSON, ranking oracle, IVF), weak provenance registry and dump/migration corpora.

---

## 12. Evidence commands (what was run)

```text
find tests/fixtures crates/*/tests/fixtures -type f
rg fixture|roundtrip|PROVENANCE|UPDATE_GOLDENS|cases.json (conformance-audit + crates)
read ranking_oracle.rs, semantic_ivf_roundtrip.rs, fixture.rs, extraction_goldens.rs,
     machine fixtures, graph_oracle head, eval gold loader, golden-audit PASS1/2/6
git check-ignore **/.asgrep/; git ls-files tests/fixtures crates/*/tests/fixtures
python3: 12 ranking cases
```

Temporary research dumps (`_pass5_research_tmp.txt`, `_pass5_inv.txt`) may be deleted after this artifact; they are not part of the deliverable.
