# Pass 1 freeze fixture (slim)

Mirrors of rotational-code-analysis **Pass 1 / Loop 1** freeze artifacts.

- Full books: `TARGET/.rotational-code-analysis/` (gitignored; includes large snapshot/state).
- This directory intentionally omits `snapshot.json` / full `state.json` (tens of MB).

Canonical revision at freeze: see `frozen-target.md`.
