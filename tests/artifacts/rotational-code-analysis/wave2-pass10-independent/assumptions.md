# Assumptions (skeptic)

1. Originator RESULT.md files are **hypotheses**, not proof. Verdicts require fresh source locators + fresh RCH exit 0.
2. Option C lite (writer_generation) closes only peers that **poll** the stamp (MCP/CM Searcher caches). Third-party holders remain out of claim scope.
3. `Path::starts_with` after canonicalize is component-wise containment (same MCP jail pattern) — not string-prefix.
4. Queued pass-9 GAPs (embed HTTP timeout/body, pattern unbounded read, CM soft-timeout orphan) remain availability/saturation unless independently elevated this pass — they were not.
5. `RCH_CANONICAL_PROJECT_ROOT=/Users/aditya` required on this host (Developer path realpath escapes default AI topology).
