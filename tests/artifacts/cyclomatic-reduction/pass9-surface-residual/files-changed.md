# Files changed (Pass 9)

| Path | Change |
|---|---|
| `crates/ast-sgrep-cli/src/bench.rs` | Shared `enforce_bench_ratchet` + `print_ast_grep_human`; suite skip JSON reuses `add_index_json` |
| `crates/ast-sgrep-cli/src/search_cmd.rs` | `uses_semantic_channel` predicate consolidate for `run_search` |

No changes to launcher, mcp, lsp, lang, or `lib.rs` (refuse / Keep).
