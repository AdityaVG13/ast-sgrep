# Skill Loop Progress
# Skill: module-coupling-topology
# Skill path: /Users/aditya/AI/JeffreySkills/_custom/module-coupling-topology
# Target: /Users/aditya/Developer/ast-sgrep (entire repo)
# Branch: perf/software-optimization
# Mode: N until absolute convergence (cap 12); stop on two consecutive ZERO-CHANGE
# Deliverable: findings as beads via `br create` (upgrade open items in place when present)
# Product edits: NO unless a pass explicitly authorizes a Bill-gated edge-cut (default: baseline + classify + queue only)
# Authoritative for this loop: this file (do not clobber .skill-loop-progress.md — that is cyclomatic-reduction)
# Total Passes: 12
# Started: 2026-08-10T23:57:03Z
# Note: zerostack unavailable (fszero/tokenzero engines missing); native shell used

## Status: IN PROGRESS — Pass 1 of 12

## Missions
1. Preflight + target resolve + smoke measure: resolve joint, run preflight/smoke measure, write books under `.module-coupling-topology/`
2. Full baseline graph: measure_coupling fan/SCC/DSM raw JSON + baseline report + shape probe green
3. Band map + coupling ledger: DSM bands, layer map, hotspot ledger in 03-coupling-ledger.md
4. Classify edges: Class: essential_shared | accidental_edge | cycle_debt | fenceable on edge cards
5. SCC / cycle_debt deep dive: enumerate SCCs and cycle pairs; file beads for break/defer candidates
6. Fan-out hotspots + layer violations: high fan-out modules and illegal downward/upward edges → beads
7. Fan-in hubs + essential kernels: Keep/Fence decisions for shared kernels; refuse false-simple cuts → beads
8. Boundary-crossing edges: crate/package public-surface fences and deep-import leaks → beads
9. Edge cards + technique recommendations: name TECHNIQUES without product edits; plan sequence in 05-decouple-plan.md
10. Bead queue upgrade + dedupe: br create/upgrade; merge duplicates; work-queue INDEX
11. Bill re-measure + residual NEXT_PASS: re-measure graph; confirm no secret rise; residual deferred edges
12. Absolute convergence rescan: full re-sweep; ZERO new findings required for convergence pair

## Completed Passes
(none yet)

## Convergence tracking
- Consecutive ZERO-CHANGE count: 0
- Beads created this loop: (none yet)
