# cargo-geiger baseline (`l115`)

First-party policy: `unsafe_code = "forbid"` on product crates; sealed exception
`ast-sgrep-mmap` only (`scripts/verify-forbid-soundness`).

Dependency inventory (informational — run locally when auditing):

```bash
cargo install cargo-geiger --locked
cargo geiger --workspace -j1 || true
```

Expected: zero `unsafe` in `ast-sgrep-*` product sources except
`crates/ast-sgrep-mmap/src/lib.rs`. Dependency crates (rusqlite, ort, tree-sitter,
memmap2, …) may report unsafe; that is tracked by `cargo audit`, not
forbid-soundness.

Last reviewed on PR #21 quality-batch.
